---
phase: 04-pillar-compliance-operations
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/controls/pillar-4-operations/4.1-admin-settings-feature-management.md
  - docs/controls/pillar-4-operations/4.2-teams-meetings-governance.md
  - docs/playbooks/control-implementations/4.1/portal-walkthrough.md
  - docs/playbooks/control-implementations/4.1/powershell-setup.md
  - docs/playbooks/control-implementations/4.1/verification-testing.md
  - docs/playbooks/control-implementations/4.1/troubleshooting.md
  - docs/playbooks/control-implementations/4.2/portal-walkthrough.md
  - docs/playbooks/control-implementations/4.2/powershell-setup.md
  - docs/playbooks/control-implementations/4.2/verification-testing.md
  - docs/playbooks/control-implementations/4.2/troubleshooting.md
autonomous: true
requirements:
  - P4-01
  - P4-02

must_haves:
  truths:
    - "Control 4.1 documents the Copilot overview dashboard, Copilot Control System branding, portal consolidation into MAC, Copilot for Admins, and Baseline Security Mode"
    - "Control 4.1 adopts 'Copilot Control System' branding with old name referenced once for continuity"
    - "Control 4.2 documents the Teams Copilot default change from EnabledWithTranscript to Enabled (March 2026) with an ACTION REQUIRED compliance-critical callout box"
    - "Control 4.2 callout box includes specific Teams PowerShell cmdlet to enforce transcript retention"
    - "Control 4.2 callout box cites specific recordkeeping regulations"
  artifacts:
    - path: "docs/controls/pillar-4-operations/4.1-admin-settings-feature-management.md"
      provides: "Updated admin settings with Copilot Control System and dashboard"
      contains: "Copilot Control System"
    - path: "docs/controls/pillar-4-operations/4.2-teams-meetings-governance.md"
      provides: "Updated Teams governance with default change callout"
      contains: "EnabledWithTranscript"
  key_links:
    - from: "docs/controls/pillar-4-operations/4.2-teams-meetings-governance.md"
      to: "docs/controls/pillar-3-compliance/3.11-record-keeping.md"
      via: "Teams default change has recordkeeping compliance impact documented in 3.11"
      pattern: "recordkeeping|17a-4"
    - from: "docs/controls/pillar-4-operations/4.1-admin-settings-feature-management.md"
      to: "docs/playbooks/control-implementations/4.1/portal-walkthrough.md"
      via: "MAC Copilot dashboard navigation"
      pattern: "Copilot.*dashboard|MAC.*Copilot"
---

<objective>
Update Pillar 4 controls 4.1 (Admin Settings) and 4.2 (Teams Meetings Governance) and all their playbooks to reflect Copilot Control System branding, the MAC overview dashboard, and the critical Teams Copilot default change.

Purpose: Control 4.1 needs the Copilot Control System branding update and new admin capabilities. Control 4.2 contains the most compliance-critical update in Phase 4 — the Teams default change that directly impacts FSI recordkeeping. The SC-4 success criterion hinges on this plan.

Output: 2 updated control documents + 8 updated playbook files
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pillar-compliance-operations/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update control 4.1 with Copilot Control System and admin capabilities + playbook alignment</name>
  <files>
    docs/controls/pillar-4-operations/4.1-admin-settings-feature-management.md
    docs/playbooks/control-implementations/4.1/portal-walkthrough.md
    docs/playbooks/control-implementations/4.1/powershell-setup.md
    docs/playbooks/control-implementations/4.1/verification-testing.md
    docs/playbooks/control-implementations/4.1/troubleshooting.md
  </files>
  <action>
    **Control 4.1 updates (P4-01):**
    - **Adopt "Copilot Control System" branding** per locked decision (same pattern as Phase 2 terminology corrections):
      - First mention: "The Microsoft 365 Copilot Control System (formerly the set of distributed Copilot admin controls across M365 Admin Center, Teams Admin Center, and individual app admin centers) provides..." Reference the old approach once for continuity, then use "Copilot Control System" throughout.
      - The Copilot Control System is Microsoft's unified branding for all administrative governance capabilities across Microsoft 365 Copilot.

    - Document the **Copilot overview dashboard** in MAC:
      - Microsoft has introduced a dedicated Copilot dashboard at **M365 Admin Center > Copilot > Overview** that provides:
        - License assignment and utilization summary
        - Security posture overview (links to DSPM, DLP, sensitivity labels)
        - Usage analytics highlights
        - Recommended actions for governance gaps
      - This dashboard consolidates what previously required navigating multiple admin centers.
      - Add regulatory framing: SOX 404 IT general controls benefit from centralized monitoring dashboards that provide evidence of ongoing governance oversight.

    - Document **portal consolidation into MAC**:
      - Microsoft is progressively consolidating Copilot governance controls into the Microsoft 365 Admin Center (MAC) under the Copilot section. Update the per-application feature toggles table to reflect any settings that have moved from individual app admin centers to MAC > Copilot.

    - Document **Copilot for Admins**:
      - Microsoft 365 Copilot is now available to assist administrators within the MAC, providing natural language configuration assistance, policy recommendation summaries, and troubleshooting guidance.
      - Add FSI governance note: Copilot-assisted admin changes still require the same change management approval process — the AI assistant does not bypass CAB or segregation of duties requirements.

    - Document **Baseline Security Mode**:
      - Microsoft offers a Baseline Security Mode for Copilot that applies a set of recommended security defaults (sensitivity label requirements, DLP policies, Conditional Access) as a starting configuration.
      - Add tier guidance: Baseline: enable Baseline Security Mode as starting point; Recommended: customize beyond baseline per firm risk profile; Regulated: fully customized configuration with documented deviation from baseline where business needs require.

    **All 4.1 playbooks:**
    - portal-walkthrough: Rewrite navigation to start from MAC > Copilot > Overview dashboard. Walk through dashboard sections and link to detailed configuration areas.
    - powershell-setup: Add any new cmdlets for Copilot Control System configuration. Update Get-CopilotPolicy or equivalent cmdlets if naming has changed.
    - verification-testing: Add test cases for dashboard accessibility, Baseline Security Mode verification, Copilot for Admins availability.
    - troubleshooting: Add common issues — dashboard not showing all data (licensing), Baseline Security Mode conflicts with existing policies, Copilot for Admins not available (licensing/role requirements).

    **Integration rules:**
    - "Copilot Control System" on first mention with old approach referenced once; then use the new name throughout.
    - Seamless integration — no version markers.
    - Match existing table format.
  </action>
  <verify>
    - grep for "Copilot Control System" in 4.1 control doc (must exist)
    - grep for "dashboard\|Overview" in 4.1 control doc (must exist, Copilot dashboard)
    - grep for "Copilot for Admins\|admin.*assist" in 4.1 control doc (must exist)
    - grep for "Baseline Security Mode" in 4.1 control doc (must exist)
    - grep for "SOX.*404\|SOX Section 404" in 4.1 control doc (must exist, regulatory citation)
    - grep for "MAC.*Copilot.*Overview\|Copilot.*Overview" in 4.1 portal-walkthrough (must exist)
    - grep for "BizChat\|M365 Chat" in all modified files (must NOT exist)
  </verify>
  <done>
    Control 4.1 adopts Copilot Control System branding, documents the MAC Copilot overview dashboard, portal consolidation, Copilot for Admins, and Baseline Security Mode with tier-consistent recommendations. All 4 playbooks reflect the new admin experience.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update control 4.2 with Teams Copilot default change and FSI recordkeeping impact + playbook alignment</name>
  <files>
    docs/controls/pillar-4-operations/4.2-teams-meetings-governance.md
    docs/playbooks/control-implementations/4.2/portal-walkthrough.md
    docs/playbooks/control-implementations/4.2/powershell-setup.md
    docs/playbooks/control-implementations/4.2/verification-testing.md
    docs/playbooks/control-implementations/4.2/troubleshooting.md
  </files>
  <action>
    **Control 4.2 updates (P4-02) — SC-4 Critical:**
    This is the most compliance-critical update in Phase 4. Per locked decisions:
    - Compliance-critical callout box with "ACTION REQUIRED" level framing
    - Include specific Teams PowerShell cmdlet inline (not deferred to playbook)
    - Date-specific framing: "Effective March 2026, Microsoft changed the default..."

    - Add a **compliance-critical callout box** using MkDocs admonition syntax:

      ```markdown
      !!! danger "ACTION REQUIRED: Teams Copilot Default Change — FSI Recordkeeping Impact"
          **Effective March 2026**, Microsoft changed the default Copilot in Teams meetings policy from `EnabledWithTranscript` to `Enabled`. Under the previous default, Copilot in Teams meetings required active transcription, ensuring that all meeting content processed by Copilot was simultaneously captured as a transcript — a critical component of FSI recordkeeping infrastructure. Under the new `Enabled` default, Copilot can operate in meetings **without transcription**, meaning Copilot-generated meeting summaries and action items may exist without a corresponding verbatim transcript record.

          **For regulated financial institutions**, this change creates an immediate recordkeeping compliance gap. SEC Rule 17a-4(b)(4) requires preservation of business communications for three years. FINRA Rule 4511 requires members to preserve books and records as prescribed by FINRA and applicable regulations. FINRA Rule 3110(b)(4) requires supervisory review of communications. Without transcription, Copilot meeting artifacts (summaries, action items) may lack the underlying verbatim record needed to demonstrate the accuracy and completeness of the AI-generated summary.

          **Remediation:** Enforce the `EnabledWithTranscript` setting via Teams meeting policy to maintain the transcript-coupled behavior:

          ```powershell
          Set-CsTeamsMeetingPolicy -Identity "FSI-Regulated-Policy" -CopilotWithoutTranscript Disabled
          ```

          Apply this policy to all user groups where meeting recordkeeping obligations apply. Verify enforcement with:

          ```powershell
          Get-CsTeamsMeetingPolicy -Identity "FSI-Regulated-Policy" | Select-Object CopilotWithoutTranscript
          ```

          This must return `Disabled` to confirm that Copilot requires transcription in regulated meetings.
      ```

    - Add context around the callout explaining the change in detail:
      - `EnabledWithTranscript`: Copilot only activates when transcription is running. All Copilot-processed meeting content has a corresponding transcript. This is the recordkeeping-safe configuration.
      - `Enabled`: Copilot can activate regardless of transcription status. Copilot-generated artifacts may exist without underlying transcript evidence. This is the new Microsoft default.
      - Document the specific regulatory risk: without transcription, firms cannot verify the accuracy of Copilot meeting summaries, cannot produce verbatim meeting records upon regulatory demand, and may face examination deficiencies for incomplete meeting recordkeeping.

    - Add to the existing Copilot Recap Features section: a note that all recap features (Intelligent Recap, Meeting Notes, Action Items, etc.) are available under both settings, but only `EnabledWithTranscript` guarantees the underlying transcript exists for regulatory compliance purposes.

    - Update tier recommendations for this specific setting:
      - Baseline: Enforce `EnabledWithTranscript` for all users (override Microsoft default)
      - Recommended: Enforce `EnabledWithTranscript` + auto-transcription for all scheduled meetings involving client discussions
      - Regulated: Enforce `EnabledWithTranscript` + auto-transcription + auto-recording for all meetings involving regulated activities

    **All 4.2 playbooks:**
    - portal-walkthrough: Add step-by-step for configuring the Teams meeting policy to override the default via Teams Admin Center > Meetings > Meeting Policies.
    - powershell-setup: Add the full PowerShell remediation script including policy creation, user group assignment, and verification.
    - verification-testing: Add critical test case: verify that after policy application, Copilot does not activate in meetings without transcription. Add audit log verification that transcript events accompany Copilot events.
    - troubleshooting: Add common issues — policy not applying (assignment delay), Copilot still working without transcript (policy priority), users seeing different behavior (per-user vs org-wide policy inheritance).

    **Integration rules:**
    - The callout box MUST use `!!! danger` (not `!!! warning`) — per locked decision, this is "ACTION REQUIRED" level.
    - PowerShell cmdlet included inline in the callout box per locked decision.
    - Date-specific framing: "Effective March 2026" not vague "recently changed."
    - All regulatory citations use section number + context sentence.
    - Seamless integration — no version markers.
  </action>
  <verify>
    - grep for "EnabledWithTranscript" in 4.2 control doc (must exist)
    - grep for "ACTION REQUIRED\|danger" in 4.2 control doc (must exist, critical callout)
    - grep for "March 2026" in 4.2 control doc (must exist, date-specific framing)
    - grep for "Set-CsTeamsMeetingPolicy" in 4.2 control doc (must exist, inline PowerShell per decision)
    - grep for "CopilotWithoutTranscript" in 4.2 control doc (must exist)
    - grep for "17a-4\|4511\|3110" in 4.2 control doc (must exist, regulatory citations)
    - grep for "Set-CsTeamsMeetingPolicy" in 4.2 powershell-setup (must exist)
    - grep for "BizChat\|M365 Chat" in all modified files (must NOT exist)
  </verify>
  <done>
    Control 4.2 contains the compliance-critical ACTION REQUIRED callout box documenting the Teams Copilot default change (EnabledWithTranscript to Enabled, March 2026) with inline PowerShell remediation, specific regulatory citations (SEC 17a-4, FINRA 4511, FINRA 3110), and tier-consistent enforcement recommendations. All 4 playbooks reflect the remediation workflow.
  </done>
</task>

</tasks>

<verification>
- Both controls (4.1, 4.2) reflect their P4-01/P4-02 requirement updates
- Copilot Control System branding adopted in 4.1 with old name referenced once
- SC-4: Teams default change documented with ACTION REQUIRED callout, inline PowerShell, and regulatory citations
- Governance tiers updated consistently
- No deprecated terms in any modified file
</verification>

<success_criteria>
- P4-01: 4.1 contains Copilot Control System, overview dashboard, Copilot for Admins, Baseline Security Mode
- P4-02: 4.2 contains the Teams default change ACTION REQUIRED callout with inline PowerShell and regulatory citations (SC-4 critical)
- Copilot Control System branding pattern matches Phase 2 terminology approach
</success_criteria>

<output>
After completion, create `.planning/phases/04-pillar-compliance-operations/04-05-SUMMARY.md`
</output>
